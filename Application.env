# Allow external requests, such as those from ngrok, by disabling the host check
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Specify the development server host (bind to all network interfaces)
HOST=0.0.0.0

# Optionally, set the public URL for external access
# Replace <your-ngrok-url> with the actual ngrok forwarding URL
# Example: PUBLIC_URL=https://9201-197-58-86-223.ngrok-free.app/
PUBLIC_URL=<your-ngrok-url>
